import React from 'react';
import CustomSubscription from "@/components/page-ui/custom-subscription";

const Page = () => {
    return (
        <div className={`min-h-screen w-full flex items-center justify-center `}>
            <CustomSubscription/>
        </div>
    );
};

export default Page;