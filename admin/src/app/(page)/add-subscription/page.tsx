import React from 'react';
import SubscriptionForm from "@/components/page-ui/add-subscription";

const AddSubcription = () => {
    return (
        <div className={`w-full min-h-screen flex items-center bg-white`}>
          <SubscriptionForm/>
        </div>
    );
};

export default AddSubcription;